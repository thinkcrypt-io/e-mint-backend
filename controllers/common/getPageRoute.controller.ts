// One implementation: the admin routes apply TableConfig page toggles, and
// that lookup lives in the library copy.
export { default } from '../../library/controllers/config/getPageRoute.controller.js';
