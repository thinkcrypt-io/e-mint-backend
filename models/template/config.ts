const fields = ['sample'];

const tableFields = ['sample'];

const formFields = [
	{
		sectionTitle: 'Sample',
		fields: ['sample', 'sample', ['sample', 'sample']],
	},
];

const config = {
	fields,
	table: tableFields,
	form: formFields,
};

export default config;
